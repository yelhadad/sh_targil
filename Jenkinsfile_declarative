pipeline {
    // need to add label somehow
    agent {
        docker { image 'yelhadad/shabak:5'
                 args '--privileged'   
         }
        //label 'zip-job-docker'
    }

    stages {
        stage('Build') {
            steps {
                sh 'python3 /tmp/zip_job.py'
            }
        }
    
        stage('Publish') {
            steps {

                //setting vertion env inside the pipeline
                env.VERSION = sh(script:’echo $VERSION’, returnStdout: true).trim()
                echo "${VERSION}"

                // coping the created files (artifactory plugin does not reconize them on temp)
                sh 'mkdir ziped_files'
                sh 'cp -r /tmp/ziped_files .'
                sh 'ls ziped_files'
                
                rtUpload (
                    serverId: 'artifactory',
                    failNoOp: true,
                    spec: '''{
                        "files": [
                         {
                            "pattern": "ziped_files/*.zip",
                            "target": "binary-storage/test_zip/"
                          }
                        ]
                    }''',)
            }
      }

    }
    post {
            always {
                echo 'cleaning workspace'
                deleteDir() /* clean up our workspace */
            }
            success {
                echo 'sending success job email'
                emailext body: 'pipeline succeeded',
                    subject: 'pipeline succeeded',
                    to: 'yoave14@gmail.com'
            }
            failure {
                echo 'sending failure job email'
                emailext body: 'pipeline failed',
                    subject: 'pipeline failed',
                    to: 'yoave14@gmail.com'
            }
      }
}